function [U,V, M,O]=OF(F1,F2, Sigma_S, Region)
% Smooth the figures using sigma (the special domain)
% Smooth the image in the temporal domain..?
% Compute A and b
% Compute G
% If rank(G)=2, compute u and v